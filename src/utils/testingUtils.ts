// export const makeStylesMock = (classes) => () => {
//   const mockedClasses = {};
//   Object.keys(classes).forEach((key) => {
//     mockedClasses[key] = String(key);
//   });
//   return mockedClasses;
// };
